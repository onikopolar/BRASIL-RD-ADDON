import { PutioAPIClient } from '../../client';
import { ISharingCloneResponse, ISharingCloneInfoResponse } from './types';
export default class Sharing {
    private client;
    constructor(client: PutioAPIClient);
    Clone({ ids, cursor, excludeIds, parentId, }: {
        ids?: number[];
        cursor?: string;
        excludeIds?: number[];
        parentId?: number;
    }): Promise<import("../..").IPutioAPIClientResponse<ISharingCloneResponse>>;
    CloneInfo(cloneInfoId: number): Promise<import("../..").IPutioAPIClientResponse<ISharingCloneInfoResponse>>;
}
